# Todo In‑Memory CLI (Phase I)

Run the app:

python -m src

This project implements a minimal in-memory todo CLI for the Phase I exercise. See specs/001-phase-todo-memory for plan, tasks, and quickstart.
